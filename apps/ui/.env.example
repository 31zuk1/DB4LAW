# Absolute or workspace-relative path to Vault root.
# Default search order (when empty): ../../Vault, ../../vault, ../../data/vault
VAULT_PATH=../../Vault

# Optional path to targets.yaml.
# When present, search result scope is restricted to listed law_id entries.
TARGETS_PATH=../../targets.yaml
